import { Request, Response } from 'express'; // Importação do express module

export const search = (req: Request, res: Response) => { // Função que renderiza a pesquisa
    //res.render('pages/search'); // Renderiza a página home.mustache
}